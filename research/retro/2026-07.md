# Gardener retro — 2026-07

First run of routine 05 (`automations/05-gardener.md`). No prior retro exists, so this
month sets the baseline; the "three consecutive misses" escalation in `docs/pipeline.md`
§9 can't yet apply to anything reported here.

## 1. Throughput report

**Published in July** (frontmatter `date`, both `en`/`zh` present for all five):

| Post | Tier | Date | Draft PR → merge |
|---|---|---|---|
| [pull-requests-are-knowledge-imports](../../src/content/posts/pull-requests-are-knowledge-imports.en.md) | Essay | 2026-07-03 | 7.0 days (PR #32) |
| [codebase-maps-are-agent-interfaces](../../src/content/posts/codebase-maps-are-agent-interfaces.en.md) | Essay | 2026-07-07 | 2.9 days (PR #34) |
| [agent-prs-need-traffic-control](../../src/content/posts/agent-prs-need-traffic-control.en.md) | Essay | 2026-07-10 | 0.4 days (PR #38) |
| [taste-is-judgment](../../src/content/posts/taste-is-judgment.en.md) | Note | 2026-07-20 | 0.2 days (PR #49) |
| [taste-is-a-bet](../../src/content/posts/taste-is-a-bet.en.md) | Essay | 2026-07-31 | 0.9 days (PR #54, merged 2026-08-01 00:30 Shanghai — spans the month boundary) |

4 essays + 1 note. Median draft PR → merge: **0.9 days**. Both languages shipped in the
same PR for all five — 100%.

**Backlog flow (July):**
- Added: 12 candidates (3 scout runs of 4, on 07-03, 07-08, 07-10).
- Drafted: 1 of those 12 (`agent-prs-need-traffic-control`, same day it was added). The
  other 4 published pieces came from backlog items added in June or from an interview,
  not from July's scout batches.
- Expired: **0 marked in July**, even though several June/July items crossed the 21-day
  line during the month. See §4 — the scout didn't run again after 07-10 until 08-05, so
  nothing aged out until this week's catch-up run retroactively expired 15 items at once.
- Killed (ship-gate >14-day closes): 0.

**Origination ratio:** 4 of 5 published pieces trace to author input — `pull-requests-are-knowledge-imports`
and `agent-prs-need-traffic-control` both extend named 2026-06-19 inbox sparks (per their
own "Author hook" fields), and `taste-is-judgment` + `taste-is-a-bet` both come straight
out of the answered `2026-07-20-taste-beyond-creatives` interview. Only
`codebase-maps-are-agent-interfaces` is a pure scout find anchored to a *published post*
rather than to an inbox spark or interview answer, so it doesn't count toward this metric
under the §9 definition. **80%**, against a 60% target.

**Verdict against `docs/pipeline.md` §9:**

| Metric | Target | July | Verdict |
|---|---|---|---|
| Published pieces / month | ≥3, ≥1 essay | 5 (4 essay, 1 note) | Met |
| Median days, draft PR → published | ≤7 | 0.9 | Met |
| Posts with both languages | 100% | 100% (5/5) | Met |
| Pieces rooted in author input | ≥60% | 80% (4/5) | Met |
| Backlog items older than 21 days | 0 | **Missed in substance** — nothing was *marked* expired past the line during July, but only because the enforcement step didn't run; several items sat unexpired well past 21 days until 08-05 caught them all at once. | Missed |
| Drafter runs producing nothing | 0 | 0 literal empty-handed runs, but only because **the drafter didn't run at all** for roughly three weeks (see below) — a stricter failure than the metric as worded catches. | Technically met, substantively worse |

**The one finding that matters more than the numbers:** the content metrics above look
healthy, but most of July's output did not come from the autonomous weekly loop
described in `docs/pipeline.md` §4. The 2026-07-31 commit "Restart the stalled pipeline"
found that routines 01–05 existed only as prompt files in `automations/` — nothing had
ever scheduled them to run. `taste-is-judgment` was "fast-forwarded by hand" on 07-20,
and the `taste-is-a-bet` essay only got drafted on 07-31 because a human noticed the
07-27 green light had sat through a Thursday slot with nothing on the other end. Between
the 07-10 scout run and the 07-31 restart — three weeks — no scout, interviewer, drafter,
or ship-gate run happened at all. §8's own warning ("creating the routine and verifying
its first run are two separate steps") is exactly what was skipped. The July numbers are
good because of manual catch-up, not because the loop was running. **This is month one of
a possible miss streak on the two failed metrics above — both are really the same root
cause (the scheduler gap), and both should be watched again in August now that the crons
are supposedly wired up.**

## 2. Archive mining

Checked both frontmatter `prediction` blocks and the consulting-barbell prediction
tracker for approaching test dates:

- `agent-coordination-debt` — "absorption metrics" prediction, tests by end of 2027.
- `consulting-barbell` — EM promotion-rate compression prediction, tests by end of 2027
  (falsifier: end of 2028).
- `taste-is-a-bet` — "taste-copying as a data-collection product" prediction, tests by
  end of 2028.

None are due or imminently approaching (nearest is 16 months out), so no Tracker
candidates were added to the backlog this run. Light web checks on the two nearest-term
theses turned up recent enterprise-AI adoption data (McKinsey: no business function has
more than 10% of orgs scaling agents; ~88% of agent pilots don't reach production) that
leans in the direction of the coordination-debt thesis without being a clean test of it —
worth another look when closer to the 2027 date rather than acting on it now.

## 3. Voiceprint maintenance

Mined the two interview files on record (`2026-07-20-taste-beyond-creatives`, answered
in July; `2026-08-05-chip-design-agents-buy-trust-with-physics-not-permission`, answered
today) plus the published `taste-is-a-bet`/`taste-is-judgment` pair for recurring
patterns, since this is the first gardener run and nothing has been mined before. Added
3 new entries to `voice.md` `## Proposed`:

- A **stance** on the taste/judgment split, now taken in public in `taste-is-a-bet`.
- A **signature move** (repeating a cliché word three times to enact its own emptiness) —
  present in the raw 2026-07-17 inbox spark ("taste taste taste") and carried into the
  published piece verbatim in Chinese ("品味品味品味"). This is the first real evidence
  for the placeholder "translate a buzzword into its mechanism" example already scaffolded
  in `voice.md`'s Signature moves section — it's a distinct move from that one, not a
  duplicate.
- A **signature move**, the aphoristic form of that same "name the ugly mechanism" habit,
  from the chip-design interview's Q1 answer ("every verification gate is theater until
  someone gets fired for skipping it").

**Not distilled, left as raw records:** the 3 `A/B choice` entries the Desk appended
today from PR #58. Distillation is for *recurring* signal pointing the same direction,
and this is the first round of A/B data logged at all — one instance isn't a pattern
yet. One flag for whoever reviews this next: the "en closer" record reads more like
scope/thesis feedback for the drafter (the reader wanted a wider closing claim, not a
narrower one) than a voice/style signal, so it may not belong in the voiceprint at all —
worth a second look before it's forced into a rule it doesn't fit. The "zh 结尾" record,
by contrast, is a live confirmation of the em-dash rule already written into the
Rhythm notes section — no new entry needed there, but it's a good sign the existing rule
is correct.

No Proposed entry is old enough to hit the two-month deletion clock yet (oldest is
2026-07-17, 19 days).

## 4. Hygiene

- **Backlog expiry:** clean as of this run. The 08-05 scout swept 15 stale items
  (including everything added in June and the first three July batches) in one pass; the
  only two open items left are both from today (08-05), well inside the 21-day window.
  The gap this exposes is upstream of "hygiene" — see §1's finding about the scheduler.
- **Glossary consistency:** no violations of documented renderings found. One thing worth
  the author's attention, not a bug: `agent` vs. `智能体` usage is split hard by post —
  some essays (`agent-prs-need-traffic-control`, `pull-requests-are-knowledge-imports`)
  use the English word almost exclusively, others (`agent-coordination-debt`,
  `helpful-agents-authorization-bug`) use `智能体` almost exclusively. The glossary
  already sanctions this as a per-sentence rhythm call, so this isn't flagged as an error,
  just noted in case the spread is wider than intended.
- **Dead-link scan:** this session's `WebFetch` got HTTP 403 on every external domain
  tried (arxiv.org, paulgraham.com), which looks like a tooling/proxy restriction rather
  than a real outage — a `WebSearch` cross-check confirmed the paulgraham.com source is
  live. Given that, a real dead-link pass wasn't possible from this session; flagging
  rather than claiming a scan that didn't actually run. Worth running with a tool that
  isn't proxy-blocked before next month's gardener.
- **Stale unanswered interviews (>60 days):** none. Both interview files on record have
  answers.

## Close

Committed to `main` as `gardener: 2026-07 retro`.
